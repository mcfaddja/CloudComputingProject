Copy the file into the desired directory, make sure you have the google cloud sdk installed to run the program.
follow steps here: https://cloud.google.com/sdk/downloads
Install the google cloud library in a new virtual environment and run the bigtable-test.py in the following format.

"python bigtable-test.py [project_id] [instance_id] [table name]"
